These custom data maps are used to get information which isn't available (or easily available) from DB files.
The general idea is you set a transform or filter type as DATAMAPMAPTYPE where MAPTYPE is the name of your custom transform.
If a match is hit a custom value is returned.